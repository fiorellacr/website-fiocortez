<header class="py-4">
  <div class="container flex justify-between items-center">
    <a href="/" class="logo">
      <span class="text-3xl font-bold pixel-heading">Fiorella</span>
    </a>

    <nav class="pixel-border px-4 py-2">
      <ul class="flex gap-6">
        <li><a href="#about" class="nav-link">About</a></li>
        <li><a href="#skills" class="nav-link">Skills</a></li>
        <li><a href="#portfolio" class="nav-link">Portfolio</a></li>
        <li><a href="#contact" class="nav-link">Contact</a></li>
      </ul>
    </nav>
  </div>
</header>

<style>
  header {
    padding: 1rem 0;
    position: relative;
    z-index: 10;
  }

  .logo {
    position: relative;
    display: inline-block;
    text-decoration: none;
  }

  .logo::before {
    content: "";
    position: absolute;
    bottom: -5px;
    left: 0;
    width: 100%;
    height: 4px;
    background: repeating-linear-gradient(
      to right,
      var(--color-accent-2),
      var(--color-accent-2) 4px,
      transparent 4px,
      transparent 8px
    );
  }

  nav {
    background-color: white;
  }

  .nav-link {
    color: var(--color-text);
    text-decoration: none;
    font-family: var(--font-pixel);
    font-size: 1.25rem;
    position: relative;
    transition: color 0.2s ease;
  }

  .nav-link:hover {
    color: var(--color-accent);
  }

  .nav-link:hover::after {
    content: "";
    position: absolute;
    left: 0;
    bottom: -4px;
    width: 100%;
    height: 2px;
    background-color: var(--color-accent-2);
  }

  @media (max-width: 768px) {
    nav {
      display: none;
    }
  }
</style>
